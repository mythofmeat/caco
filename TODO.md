# GUI
- [x] GUI for launching and managing WADs
- [x] The GUI should be able to be called with `caco --gui`
- [x] WADs should have thumbnails as a rule
  - [x] there may be a way to scrape the doomwiki for an image
  - [x] Downloaded WADs should have a thumbnail extracted from TITLEPIC in the WAD
    - Implemented custom Doom patch format decoder in Python (no external tools needed)
