## id24 WAD Management

### Done
- [x] `db/_id24.py`: Known MD5 hashes, filename fallback, `identify_id24()`, CRUD functions
- [x] `db/_schema.py`: Migration #17 creates `id24_wads` table
- [x] `db/__init__.py`: Re-exports all id24 functions
- [x] `config.py`: `ID24_DIR` constant, `get_id24_dir()` function
- [x] `cli/library.py`: `--id24` flag on `ls` (rich/plain/json), `--id24` option on `trash`
- [x] `cli/import_cmds.py`: Auto-detect id24 (and IWAD) on local file import, copy to managed dir
- [x] `completions/caco.fish`: `--id24` completions for `ls` and `trash`

### Future Work
- [ ] Auto-load id24res.wad during play (player.py integration)
- [ ] Detect id24 PWADs via COMPLVL lump
- [ ] Set complevel flags for id24-compatible sourceports
- [ ] TUI/GUI id24 management screens
